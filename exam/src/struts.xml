<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE struts PUBLIC
    "-//Apache Software Foundation//DTD Struts Configuration 2.5//EN"
    "http://struts.apache.org/dtds/struts-2.5.dtd">
<struts>
	<!-- 配置action后缀名为do -->
	<constant name="struts.action.extension" value="do"></constant>
	<package name="area" namespace="/area" extends="struts-default">
		<action name="*Area" class="areaAction" method="{1}">
			<result name="{1}">/area/{1}.jsp</result>
			<result name="toList" type="redirectAction">listArea.do</result>
			<allowed-methods>list,add</allowed-methods><!-- 使用通配符的请求，方法必须在此处添加上 -->
		</action>
	</package>
</struts>